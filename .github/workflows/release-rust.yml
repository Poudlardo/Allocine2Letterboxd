name: Release Rust Windows binary

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  build-windows:
    name: Build Windows x86_64 binary
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-pc-windows-msvc

      - name: Build release binary
        working-directory: rust-version
        run: cargo build --release --target x86_64-pc-windows-msvc

      - name: Rename binary
        working-directory: rust-version
        shell: pwsh
        run: Copy-Item target\x86_64-pc-windows-msvc\release\allocine2letterboxd.exe allocine2letterboxd.exe

      - name: Upload to GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: rust-version/allocine2letterboxd.exe
          generate_release_notes: true
